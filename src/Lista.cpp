//Source file

#include "Lista.h"
#include <iostream>

using namespace std;

template <typename T> void Lista<T>::insertar(const T & elemento) {
    if (lista == NULL) {
        Lista<T> aux = 
    }
}